# 接口规范差异分析报告

> 对照文档：`系统接口规范.md` v1.0（2026-03-19，已更新）  
> 分析时间：2026-04-05  
> 最后更新：2026-04-05  
> 分析范围：`voice_agent/` 模块全部 Go 源码  
> 结论：经规范更新后复核，真实待修复问题仅剩 **1 处**（S5）。L4 已修复。

---

## 待修复

无。所有可修复问题已完成。

---

## 已修复 ✅

| 编号 | 问题 | 修复方式 |
|---|---|---|
| S1 | 回调路由名称错误 | `main.go` 已注册 `POST /api/v1/voice/ppt_message` |
| S3 | KBQueryResponse 结构 | 已改为 `Summary string`（voice agent 用总结） |
| S4 | KBQueryRequest 字段 | 已有 `CollectionID`，无 `Subject`/`Filters` |
| S7 | task_created WS 消息 | `pipeline_ctx.go` 已实现 |
| M3 | requirements_summary 消息 | `pipeline_post.go` 已发送 |
| L1 | SearchRequest.SearchType | 字段本不应存在，已从代码和规范移除 |
| L4 | AddPendingQuestion 在 TTS 被打断时不调用 | `bus.go` 已移至 TTS 后立即调用，无论是否被打断 |

---

## 规范已同步，无需修复

| 编号 | 原问题描述 | 实际情况 |
|---|---|---|
| S2 | ContextMessage.Source 字段名错误 | 规范已更新为 `ActionType`/`action_type`，代码正确 |
| S8 | 需求状态机用 `"ready"` 非 `"confirming"` | 规范状态就是 `collecting → ready`，代码正确 |
| M1 | TaskRequirements 字段问题 / GetMissingFields 把可选字段当必填 | 规范 Checklist 所有字段均为 P0，代码正确 |
| M4 | task_init 入站消息未处理 | 规范中无此消息类型，原分析有误 |
| M5 | requirements_confirm 入站消息未处理 | 规范中无此消息类型，确认流程由语音对话完成 |
| L2 | MemoryExtractResponse 类型错误 | 规范就是 `[]string`，代码正确 |

---

## 范围外（不修复）

| 编号 | 问题 |
|---|---|
| S5 | asyncQuery 函数未实现（架构重构，单独任务） |
| M2 | ToPPTInitRequest 方法缺失（重构，不影响功能） |
| M6 | Memory 请求高级字段缺失（高级功能，单独任务） |
| M7 | prefillFromMemory 函数缺失（单独任务） |
| L3 | WorkingMemory.ExtractedElements 类型（breaking change，单独任务） |
| L5 | PPTMessageRequest 包含规范外字段（有意扩展，记录即可） |
